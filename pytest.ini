[pytest]
filterwarnings =
    ignore::DeprecationWarning
    ignore::UserWarning
    ignore::FutureWarning
markers =
    torch: tests that require PyTorch to be installed
    requires_torch: skip if torch is not available
